{% extends '_admin/_includes/index.html.twig.twig' %}

{% block titre %}{{ menu.getRoute }}{% endblock %}

{% block page_content %}

    {% set new = menu.getRoute %}
    <!-- PAGE-HEADER -->
    <div class="page-header" style="margin-top:-0.1rem">
        <h1 class="page-title">{{ new |capitalize }}</h1>
        <div>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="javascript:void(0)">Gestion des actes</a></li>
                <li class="breadcrumb-item active" aria-current="page">{{ new | capitalize }}</li>
            </ol>
        </div>

    </div>
    <!-- PAGE-HEADER END -->
    <!-- Row -->
    <div class="row row-sm" style="margin-top:-2rem">
        <div class="col-lg-12">

            <div class="card">
                <div class="card-header">

                    <div class="col-md-10">
                        <h3 class="card-title">{{ titre | capitalize }}</h3>
                    </div>
                    <div class="col-md-2">
                        <a class="btn btn-secondary bg-secondary-gradient offset-7" {% if new !="admin" %} href="{{ path(new ~ '_new') }}" {% endif%} {% if modal=="modal" %} data-bs-toggle="modal" data-bs-target="#extralargemodal" {% endif %}>
                            <i class="fe fe-plus"></i>Nouveau
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table id="example2" class="table table-bordered text-nowrap border-bottom">
                            <thead>

                            {% include '_admin/_tableau/header.html.twig' with {'tableau':tableau  }%}

                            </thead>
                            <tbody>
                            {% for data in pagination %}
                                <tr>
                                    <td >
                                        {{ data.titre }}
                                    </td>
                                    <td >
                                        {{ data.ordre }}
                                    </td>
                                    {% include '_admin/_tableau/action.html.twig' with {'module':new ,'data':data }%}

                                </tr>
                            {% endfor %}

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}

{% block javascripts %}
    {{ parent() }}

{% endblock %}
